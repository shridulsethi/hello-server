# Hello Server – Spring Boot

A minimal Spring Boot REST API deployed on Render.

## Endpoints
GET /hello  
Returns a greeting message.

GET /health  
Health check endpoint.

## Tech Stack
Java 17  
Spring Boot  
Maven  
Docker  
Render

## Run locally
./mvnw clean package  
java -jar target/hello-server-0.0.1-SNAPSHOT.jar
